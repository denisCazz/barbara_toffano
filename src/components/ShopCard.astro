---
import type { Product } from '../lib/products';

interface Props {
  product: Product;
}
const { product } = Astro.props;

const typeLabelMap: Record<Product['category'], string> = {
  consulto: 'Consulto',
  meditazione: 'Meditazione',
  lettura: 'Lettura',
  vendita: 'Vendita',
};
const typeColorMap: Record<Product['category'], string> = {
  consulto: 'badge-consulto',
  meditazione: 'badge-meditazione',
  lettura: 'badge-lettura',
  vendita: 'badge-vendita',
};
---

<article class="shop-card">
  <div class="card-header">
    <div class="icon-area">{product.icon}</div>
    <div class="card-meta">
      <span class={`type-badge ${typeColorMap[product.category]}`}>{typeLabelMap[product.category]}</span>
      <h3>{product.name}</h3>
    </div>
  </div>

  <p class="desc">{product.description}</p>

  <ul class="details">
    {product.details.map((d) => (
      <li><span class="check">◆</span>{d}</li>
    ))}
  </ul>

  <div class="card-footer">
    <span class="price">€{product.price.toFixed(2)}</span>
    <a href={`/checkout?product=${product.id}`} class="buy-btn">
      Acquista <span class="arr">→</span>
    </a>
  </div>
</article>

<style>
  .shop-card {
    background: var(--card-bg, #fffcf5);
    border: 1px solid rgba(185,146,58,0.2);
    border-radius: 16px;
    padding: 1.75rem;
    display: flex;
    flex-direction: column;
    height: 100%;
    box-sizing: border-box;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }
  .shop-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 16px 48px rgba(185,146,58,0.11);
  }

  /* ── Header: icon + meta ── */
  .card-header {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    margin-bottom: 1.25rem;
    padding-bottom: 1.25rem;
    border-bottom: 1px solid rgba(185,146,58,0.13);
  }

  .icon-area {
    font-size: 2rem;
    width: 58px;
    height: 58px;
    border-radius: 14px;
    background: linear-gradient(135deg, rgba(167,139,250,0.18), rgba(185,146,58,0.12));
    border: 1px solid rgba(185,146,58,0.22);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    transition: transform 0.3s;
  }
  .shop-card:hover .icon-area { transform: scale(1.06); }

  .card-meta {
    display: flex;
    flex-direction: column;
    gap: 0.45rem;
    min-width: 0;
  }

  .type-badge {
    font-family: 'Cinzel', serif;
    font-size: 0.58rem;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    padding: 0.22rem 0.6rem;
    border-radius: 4px;
    display: inline-block;
    width: fit-content;
    white-space: nowrap;
  }
  .badge-consulto {
    background: rgba(124, 58, 237, 0.12);
    color: var(--accent);
    border: 1px solid rgba(124, 58, 237, 0.30);
  }
  .badge-meditazione {
    background: rgba(196, 181, 253, 0.18);
    color: var(--accent-mid);
    border: 1px solid rgba(196, 181, 253, 0.35);
  }
  .badge-lettura {
    background: rgba(107, 63, 29, 0.12);
    color: var(--gold-mid);
    border: 1px solid rgba(107, 63, 29, 0.28);
  }
  .badge-vendita {
    background: rgba(234, 88, 12, 0.12);
    color: var(--ember);
    border: 1px solid rgba(234, 88, 12, 0.30);
  }

  h3 {
    font-size: 1rem;
    color: var(--text);
    letter-spacing: 0.5px;
    margin: 0;
    line-height: 1.3;
  }

  /* ── Body ── */
  .desc {
    font-size: 0.95rem;
    color: var(--text-muted);
    line-height: 1.65;
    margin: 0 0 1rem;
    flex: 1;
  }

  .details {
    list-style: none;
    margin: 0 0 1.25rem;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }
  .details li {
    display: flex;
    align-items: center;
    gap: 0.55rem;
    font-size: 0.88rem;
    color: var(--text-muted);
  }
  .check {
    color: var(--gold);
    font-size: 0.38rem;
    flex-shrink: 0;
  }

  /* ── Footer: price + CTA ── */
  .card-footer {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    margin-top: auto;
    padding-top: 1.1rem;
    border-top: 1px solid rgba(185,146,58,0.14);
  }

  .price {
    font-family: 'Cinzel', serif;
    font-size: 1.7rem;
    color: var(--ember);
    font-weight: 600;
    letter-spacing: -0.5px;
  }

  .buy-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    padding: 0.7rem 1.5rem;
    background: var(--accent);
    color: #fff;
    border-radius: 8px;
    text-decoration: none;
    font-family: 'Cinzel', serif;
    font-size: 0.72rem;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    transition: background 0.25s, transform 0.2s;
    white-space: nowrap;
    flex-shrink: 0;
  }
  .buy-btn:hover {
    background: var(--accent-mid);
    transform: translateY(-1px);
  }
  .arr { transition: transform 0.25s; }
  .buy-btn:hover .arr { transform: translateX(4px); }
</style>
